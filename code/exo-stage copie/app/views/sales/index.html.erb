<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h2>Projets de ventes</h2>
    </div>
  </div>
  <% @sales.each do |sale| %>
    <div class="row">
      <div class="col-xs-12 col-sm-6">
        <%= link_to sale.title, sale_path(sale) %>
      </div>
      <div class="col-xs-12 col-sm-6">
        <%= number_to_currency(sale.price, precision: 0, locale: :fr, unit: '€', format: "%n %u", delimiter: ' ') %>
      </div>
    </div>
  <% end %>
</div>
